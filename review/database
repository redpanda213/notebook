1、数据库的三范式
第一，强调的是列的原子性，即数据库表的每一列都是不可分割的原子数据项。
第二，要求实体的属性完全依赖于主关键字，所谓完全依赖是指不能存在仅依赖主关键字一部分的属性。
第三，任何非主属性不依赖于其他非主属性。

2、MyISAM与InnoDB
一张自增表里面总共有 7 条数据，删除了最后 2 条数据，重启 MySQL数据库，又插入了一条数据，此时 id 是几？
答:表类型如果是MyISAM，那id就是8
如果是InnoDB，那id就是6.

3、oracle 与 mysql
优点oracle：
1）数据库特性众多，可以满足企业的多样性需求
2）处理海量数据有优势
3）提供一整套数据处理的优势
缺点oracle：
1）价格昂贵，

简而言之，就是大型跨国企业等大型企业，企业资金充足，能承担的起oracle的昂贵费用。而这些企业的数据量也是十分庞大的，更多的是关注数据的安全性和高效存储，以及出现问题时可以快速解决，可见，oracle能满足它们的基本所有需求。

而对于中小型企业或者我么那个人使用来说，oracle太笨重，且授权费用昂贵。难以负担。他们的数据量也不是特别大。Mysql就已经可以解决所有需求了。干嘛用oracle呢对不对。

4、数据库解决了哪些问题？
5、关系模型解决了什么问题？



